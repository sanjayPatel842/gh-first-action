name: first Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps : 
      - name: print greeting
        run: echo "hello worl!"
      - name : print goodbye
        run : echo "Done - bye!"
        
